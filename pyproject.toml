[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "menedpy"
version = "0.1.0"
description = "Contains various PDE solvers"
readme = "README.md"
requires-python = ">=3.9"

authors = [
  {name = "Pedro", email = "pedropasa2003@gmail.com"}
]

dependencies = [
  "numpy>=1.26"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
